---
---
<header>
    <div class="header-container">

    
    <div class="header-content">
        <div class="header-wrapper">
            
            <div class="text-content">
                <img class="logo" src="public/logo.png" alt="SVG image description">
                
                <p class="subtitle">Your gateway to the dynamic world of Technology</p>

            </div>
        </div>
    </div>
</div>
</header>

<style>
    

    @keyframes gradientFlow {
        0% {
            background-position: 0% 50%;
        }
        50% {
            background-position: 100% 50%;
        }
        100% {
            background-position: 0% 50%;
        }
    }

    body {
        margin: 0;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        background: var(--gdp-dark-blue);
        color: #fff;
    }

    header {
        padding: 2rem 0;
        text-align: center;
        border-bottom: 2px solid var(--gdp-gold);
        background: linear-gradient(
            to bottom,
            var(--gdp-blue),
            var(--gdp-dark-blue)
        );
    }

    .header-content {
        max-width: 90%;
        margin: 0 auto;
        overflow: visible;
        box-sizing: border-box;
    }

    h1 {
        font-size: 2.5rem;
        font-weight: 700;
        line-height: 1.2;
        margin: 0;
        background-image: var(--gdp-gradient);
        background-size: 200% auto;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        animation: gradientFlow 7s ease infinite;
    }

    .subtitle {
        color: var(--gdp-gold);
        font-size: 1rem;
        margin-top: 0.5rem;
        line-height: 1.4;
        text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
    }

    @media (max-width: 768px) {
        header {
            padding: 1.5rem 0;
        }

        h1 {
            font-size: 2rem;
        }

        .subtitle {
            font-size: 0.9rem;
        }
    }
    .logo {
            
            flex-shrink: 0;
        }
        .header-wrapper {
        display: flex;
        align-items: center;
        gap: 1rem;
    }

    .text-content {
        display: flex;
        flex-direction: column;
    }
    .header-container {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 100%;
    }

    
</style>
